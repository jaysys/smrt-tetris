# D1 테스트결과서

## 0. 현행화 메모
- 현행화 기준일: 2026-03-12 00:47 KST
- 본 문서는 D1 단계 시험 이력 보존 문서다.
- `01_MVP_통합_테스트계획서.md`, `02_상세_테스트케이스.md`가 최신 설계 변경을 반영해 재수립되었으므로, 본 문서는 당시 시험 기준 이력으로 해석한다.
- 최신 케이스 기준 재판정 필요 범위는 `05_현재_테스트가능범위_식별서.md`를 우선 확인한다.

## 1. 문서 목적
- 본 문서는 `D1 코어 플레이 완성` 단계에 대해 테스터가 실제 수행한 테스트 결과를 기록한다.
- 대상 범위는 `TC-002 ~ TC-008` 직접 기능 검증과 D1 공식 품질 게이트 재시험 결과다.

## 2. 실행 개요
- 1차 실행 일시: 2026-03-11 23:00 KST ~ 2026-03-11 23:30 KST
- 재시험 일시: 2026-03-11 23:40 KST ~ 2026-03-11 23:46 KST
- 실행 기준 문서:
  - `4.테스터/02_상세_테스트케이스.md`
  - `4.테스터/05_현재_테스트가능범위_식별서.md`
  - `3.개발자/07_개발관리_개발계획서.md`
  - `3.개발자/08_작업관리대장.md`
  - `3.개발자/18_테스터전달_D1재시험요청.md`
- 실행 환경:
  - 로컬 워크스페이스
  - Playwright headless
  - Node.js / pnpm 기반 명령 실행

## 3. D1 완료 주장 사전 확인
- 개발관리 문서상 현재 단계는 `D1 코어 플레이 완성`으로 기재되어 있다.
- 작업관리대장 기준 D1 핵심 선행 작업은 아래처럼 `QA_READY` 또는 `DONE` 상태를 확인했다.
  - `GE-002` 이동/회전/충돌 판정: `QA_READY`
  - `FE-002` 코어 플레이 HUD: `QA_READY`
  - `FE-003` 결과 화면/재도전: `QA_READY`
  - `BE-006` 게임 세션 종료/결과 루프 지원: `DONE`
- 개발자 재시험 요청 문서 기준 공식 게이트 수정 반영 항목은 `apps/web/src/pages/500.tsx` 추가다.

## 4. 수행 결과 요약
| 구분 | 결과 |
|---|---|
| 정식 테스트케이스 수행 | `TC-002 ~ TC-008` 7건 Pass |
| 단위 테스트 | 12건 Pass |
| 공식 빌드 | Pass on retest |
| 공식 E2E | Pass on retest |
| D1 종합 판정 | `Pass` |

## 5. 수행 테스트 상세
| 실행 ID | 대상 | 연결 테스트케이스 | 결과 | 비고 |
|---|---|---|---|---|
| D1-RUN-001 | 브라우저 직접 자동 검증 | `TC-002` | Pass | 시작 버튼으로 즉시 플레이 진입, HUD 노출 확인 |
| D1-RUN-002 | 브라우저 직접 자동 검증 | `TC-003` | Pass | 첫 방문 튜토리얼 3단계 진행 및 스킵 확인 |
| D1-RUN-003 | 브라우저 직접 자동 검증 | `TC-004` | Pass | 좌/우 이동, 회전 입력 반영 확인 |
| D1-RUN-004 | 브라우저 직접 자동 검증 | `TC-005` | Pass | Hold 동작, Hold lock, next queue, ghost 노출 확인 |
| D1-RUN-005 | 브라우저 직접 자동 검증 | `TC-006` | Pass | soft drop, hard drop, 점수 증가 확인 |
| D1-RUN-006 | 브라우저 직접 자동 검증 | `TC-007` | Pass | 실제 top out 후 결과 화면 진입 확인 |
| D1-RUN-007 | 브라우저 직접 자동 검증 | `TC-008` | Pass | 재도전 후 새 플레이 세션 진입 확인 |
| D1-RUN-008 | `pnpm test` | D1 단위 검증 | Pass | `@tetris/game-engine` 12 tests passed |
| D1-RUN-009 | `pnpm build` 1차 | D1 품질 게이트 | Fail | `@tetris/web` 빌드 실패 |
| D1-RUN-010 | `pnpm test:e2e` 1차 | D1 공식 E2E | Fail | 3 tests failed, 페이지가 `Internal Server Error` 상태 |
| D1-RUN-009R | `pnpm build` 재시험 | D1 품질 게이트 | Pass | 종료 코드 `0`, 전체 패키지 빌드 성공 |
| D1-RUN-010R | `pnpm test:e2e` 재시험 | D1 공식 E2E | Pass | 3 tests passed, 9.1s |

## 6. 테스트케이스별 결과
### 6.1 `TC-002` 게스트 즉시 플레이 시작
- 결과: Pass
- 근거:
  - 랜딩에서 시작 버튼 클릭 후 게임 화면으로 전환됨
  - `game-canvas`, `score-value`, `session-id` 노출 확인

### 6.2 `TC-003` 첫 방문 튜토리얼 스킵
- 결과: Pass
- 근거:
  - 첫 진입 시 `tutorial-overlay`가 노출됨
  - 다음 단계 진행과 `tutorial-skip-button` 동작 확인

### 6.3 `TC-004` 핵심 조작 입력 정상 동작
- 결과: Pass
- 근거:
  - 좌/우 이동 입력에 따라 보드 상태가 변함
  - 회전 입력 반영 확인

### 6.4 `TC-005` Hold 및 다음 블록/고스트 표시
- 결과: Pass
- 근거:
  - Hold 1회 성공 확인
  - 동일 턴에서 재 Hold 차단 확인
  - `hold-slot`, `next-queue` 노출 확인
  - ghost 표시 동작 확인

### 6.5 `TC-006` 라인 제거와 점수 즉시 반영
- 결과: Pass
- 근거:
  - soft drop, hard drop 이후 점수 증가 확인
  - 기록값: `scoreBefore=2`, `scoreAfter=36`

### 6.6 `TC-007` 게임 오버와 결과 화면 표시
- 결과: Pass
- 근거:
  - 실제 top out 조건 유도 후 결과 화면 전환 확인
  - 결과 문구 선두값: `종료 사유: TOP OUT`

### 6.7 `TC-008` 결과 화면 재도전
- 결과: Pass
- 근거:
  - 결과 화면 `retry-button` 동작 확인
  - 재시작 후 게임 화면 재진입 확인
  - 재도전 시 첫 방문 튜토리얼이 다시 열리지 않음

## 7. 공식 게이트 재시험 결과
### 7.1 `pnpm build`
- 1차 결과: Fail
- 재시험 결과: Pass
- 재시험 근거:
  - 종료 코드 `0`
  - `@tetris/web`, `@tetris/admin`, `@tetris/api` 포함 전체 빌드 완료
  - `@tetris/web` 기준 `/`, `/admin`, `/_not-found`, `/500` 산출 확인

### 7.2 `pnpm test:e2e`
- 1차 결과: Fail
- 재시험 결과: Pass
- 재시험 근거:
  - 종료 코드 `0`
  - Playwright 기준 3개 시나리오 모두 성공
  - 실행 요약: `3 passed (9.1s)`

## 8. 이력 메모
- D1 직접 기능 검증 `TC-002 ~ TC-008` Pass 판단은 재시험 후에도 그대로 유효하다.
- 공식 게이트 실패 이력은 있었으나, 개발자 수정 반영 후 재시험에서 모두 해소되었다.
- 따라서 D1 품질 게이트 실패는 현재 기준 열린 이슈로 유지하지 않는다.

## 9. 산출물
| 산출물 | 내용 |
|---|---|
| 직접 검증 메모 | `holdWorked=true`, `holdLocked=true`, `scoreBefore=2`, `scoreAfter=36`, `topOutLead=종료 사유: TOP OUT` |
| Playwright 최종 결과 | `3 passed (9.1s)` |
| 빌드 최종 결과 | `turbo run build --concurrency=1` 종료 코드 `0` |

## 10. 현재 판정
- D1 기능 범위 `TC-002 ~ TC-008`은 직접 검증 기준 모두 통과했다.
- 개발자 재시험 요청 기준 공식 게이트 `pnpm build`, `pnpm test:e2e`도 모두 재시험 통과했다.
- 따라서 현재 QA 판정은 아래와 같다.

| 항목 | 판정 |
|---|---|
| 코어 플레이 직접 기능 검증 | Pass |
| game-engine 단위 테스트 | Pass |
| 공식 빌드 | Pass |
| 공식 E2E | Pass |
| D1 종합 판정 | `Pass` |

## 11. 후속 조치
- D1 결과는 종결 처리 가능하다.
- D2 범위 `TC-009 ~ TC-011` 착수 가능 여부를 다음 단계 문서와 작업 상태 기준으로 확인하면 된다.

# D0 테스트결과서

## 0. 현행화 메모
- 현행화 기준일: 2026-03-12 00:47 KST
- 본 문서는 D0 단계 시험 이력 보존 문서다.
- 최신 테스트 범위 해석은 `05_현재_테스트가능범위_식별서.md`와 최신 `02_상세_테스트케이스.md`를 우선 적용한다.

## 1. 문서 목적
- 본 문서는 현재 시점에서 테스터가 실제 수행한 D0 범위 테스트 결과를 기록한다.
- 대상 범위는 현재 실행 가능한 명령 기반 검증과 랜딩/운영 프리뷰 E2E 스모크다.

## 2. 실행 개요
- 실행 일시: 2026-03-11 22:30 KST ~ 2026-03-11 22:55 KST
- 실행 기준 문서:
  - `4.테스터/02_상세_테스트케이스.md`
  - `4.테스터/05_현재_테스트가능범위_식별서.md`
- 실행 환경:
  - 로컬 워크스페이스
  - Playwright headless
  - Node.js / pnpm 기반 명령 실행
  - API 직접 호출은 권한 확장 세션에서 수행

## 3. 수행 결과 요약
| 구분 | 결과 |
|---|---|
| 정식 테스트케이스 수행 | `TC-001` Pass |
| 자동화 E2E | 2건 Pass |
| 단위 테스트 | 7건 Pass |
| DB 명령 | `db:reset` Pass, `db:migrate` Pass, `db:seed` 재테스트 Pass |
| API 직접 호출 검증 | 6건 Pass |

## 4. 수행 테스트 상세
| 실행 ID | 대상 | 연결 테스트케이스 | 결과 | 비고 |
|---|---|---|---|---|
| D0-RUN-001 | `pnpm test:e2e` | `TC-001` 포함 | Pass | 2 tests passed, 3.3s |
| D0-RUN-002 | `pnpm test` | D0 단위 검증 | Pass | `@tetris/game-engine` 7 tests passed |
| D0-RUN-003 | `pnpm db:reset` | D0 DB 스모크 | Pass | SQLite reset scaffold completed |
| D0-RUN-004 | `pnpm db:migrate` | D0 DB 스모크 | Pass | `0001_init.sql` 적용 완료 |
| D0-RUN-005 | `pnpm db:seed` 1차 | D0 DB 스모크 | Fail | `no such table: players` |
| D0-RUN-005R | `pnpm db:seed` 재테스트 | D0 DB 스모크 | Pass | Seed completed |
| D0-RUN-006 | `GET /v1/health` | D0 API 스모크 | Pass | `success=true`, `service=api` 확인 |
| D0-RUN-007 | `GET /v1/bootstrap` | D0 API 계약 확인 | Pass | `guestToken`, `dailyChallenge`, `settings` 확인 |
| D0-RUN-008 | `POST /v1/game-sessions` | D0 API 계약 확인 | Pass | `sessionId`, `seed`, `configVersion`, `rules` 확인 |
| D0-RUN-009 | `GET /v1/settings` | D0 API 계약 확인 | Pass | 기본 설정 응답 확인 |
| D0-RUN-010 | `GET /v1/test/ping` 1차 | TEST_MODE 엔드포인트 | Fail | 404 Not Found |
| D0-RUN-011 | `POST /v1/test/reset` 1차 | TEST_MODE 엔드포인트 | Fail | 404 Not Found |
| D0-RUN-010R | `GET /v1/test/ping` 재시험 | TEST_MODE 엔드포인트 | Pass | `testMode=true`, `status=ok` 확인 |
| D0-RUN-011R | `POST /v1/test/reset` 재시험 | TEST_MODE 엔드포인트 | Pass | `accepted=true` 확인 |

## 5. 통과 항목
### 5.1 `TC-001` 랜딩 진입과 핵심 CTA 노출
- 결과: Pass
- 근거:
  - Playwright E2E에서 랜딩 baseline 시나리오 통과
  - `daily-banner`, `start-button`, `mode-button`, `ranking-button` 선택자 확인

### 5.2 운영 프리뷰 선택자 노출
- 결과: Pass
- 근거:
  - Playwright E2E에서 `/admin` 프리뷰 시나리오 통과
  - `create-daily-button`, `announcement-create-button`, `fraud-queue-table` 확인

### 5.3 game-engine 단위 테스트
- 결과: Pass
- 근거:
  - `board.test.ts` 5건 Pass
  - `bag.test.ts` 2건 Pass
  - 총 7건 Pass

### 5.4 DB 초기화 및 마이그레이션
- 결과: Pass
- 근거:
  - `db:reset` 성공
  - `db:migrate` 성공

### 5.5 API 직접 호출 스모크
- 결과: Pass
- 근거:
  - `GET /v1/health` 성공
  - `GET /v1/bootstrap` 성공
  - `POST /v1/game-sessions` 성공
  - `GET /v1/settings` 성공
  - `GET /v1/test/ping` 재시험 성공
  - `POST /v1/test/reset` 재시험 성공

## 6. 실패 및 보류 항목
### 6.1 `pnpm db:seed` 실패
- 결과: Resolved on retest
- 오류 요약:
  - `Error: no such table: players`
- 영향:
  - 1차 실행에서는 문서상 D0 시드 검증 완료 상태와 실제 실행 결과가 불일치했다.
  - 재테스트에서는 정상 통과했다.
- 조치 필요:
  - 동일 현상이 재발하는지 추후 회귀 확인 필요

### 6.2 TEST_MODE 엔드포인트 404
- 결과: Resolved on retest
- 오류 요약:
  - `GET /v1/test/ping` -> `404 Not Found`
  - `POST /v1/test/reset` -> `404 Not Found`
- 영향:
  - 1차 실행에서는 작업관리대장상 `BE-005 QA_READY`와 실제 런타임 응답이 불일치했다.
  - 개발자가 제시한 표준 절차 `pnpm start:api:test` 기준 재시험에서는 정상 응답을 확인했다.
- 조치 필요:
  - 재시험 표준 절차를 이후 D0/D1 API 테스트 기준 절차로 유지

## 7. 산출물 경로
| 산출물 | 경로 |
|---|---|
| Playwright HTML 리포트 | `artifacts/e2e/2026-03-11/report/index.html` |
| Playwright 콘솔 로그 | `artifacts/e2e/2026-03-11/output/landing-P0-LANDING-001-lan-7d845-xposes-stable-CTA-selectors-chromium-mobile/console.log` |
| API health 응답 | 본 실행 로그 기준 `success=true`, `service=api` |
| API bootstrap 응답 | 본 실행 로그 기준 `guestToken`, `dailyChallenge`, `settings` 포함 |
| API session 응답 | 본 실행 로그 기준 `sessionId`, `seed`, `configVersion`, `rules` 포함 |
| API test ping 응답 | 본 실행 로그 기준 `success=true`, `data.testMode=true` |
| API test reset 응답 | 본 실행 로그 기준 `success=true`, `data.accepted=true` |

## 8. 현재 판정
- D0 범위에서 일부 실행형 테스트는 수행 가능함을 확인했다.
- `db:seed`는 재테스트에서 해소되었다.
- TEST_MODE 엔드포인트도 개발자 표준 절차 기준 재시험에서 해소되었다.
- 따라서 현재 QA 판정은 아래와 같다.

| 항목 | 판정 |
|---|---|
| 랜딩/운영 프리뷰 E2E | Pass |
| 단위 테스트 | Pass |
| DB 초기화/마이그레이션 | Pass |
| DB 시드 적재 | Pass on retest |
| API 기본 엔드포인트 검증 | Pass |
| TEST_MODE 엔드포인트 검증 | Pass on retest |
| D0 종합 판정 | Pass |

## 9. 후속 조치
- D0 결과는 종결 처리 가능
- D1 완료 이후 `TC-002 ~ TC-008` 착수

# D2 UI 강화 시험결과서

## 1. 문서 목적
- 본 문서는 `3.개발자/23_테스터전달_D2_UI강화_시험요청.md` 기준으로 수행한 `FE-008 ~ FE-013` 제3자 시험 결과를 기록한다.
- 이번 시험 범위는 랜딩 Hero 강화, 모드 선택 포스터형 재디자인, 결과 CTA 구조, 랭킹 기본 구조, 설정/닉네임/Daily 바텀시트를 포함한다.

## 2. 실행 개요
- 실행 일시: 2026-03-13 00:32 KST ~ 2026-03-13 00:37 KST
- 실행 기준 문서:
  - `3.개발자/23_테스터전달_D2_UI강화_시험요청.md`
  - `4.테스터/01_MVP_통합_테스트계획서.md`
  - `4.테스터/02_상세_테스트케이스.md`
- 실행 환경:
  - 워크스페이스 루트 명령 실행
  - Playwright headless
  - 모바일 세로 `390x844`, `360x800`
  - 공개 웹 `http://127.0.0.1:3110`

## 3. 수행 결과 요약
| 구분 | 결과 |
|---|---|
| `pnpm test` | Pass |
| `pnpm build` | Pass |
| `pnpm test:e2e` | Pass |
| 랜딩 Hero/CTA 위계 | Partial Pass |
| 모드 선택 포스터형 구조 | Pass |
| 랭킹 기본 구조 | Pass |
| 결과 화면 CTA 구조 | Fail |
| 바텀시트 흐름 | Partial Pass |
| 종합 판정 | `Partial Pass` |

## 4. 수행 테스트 상세
| 실행 ID | 대상 | 결과 | 비고 |
|---|---|---|---|
| D2-UI-RUN-001 | `pnpm test` | Pass | `@tetris/game-engine` 16 tests passed |
| D2-UI-RUN-002 | `pnpm build` | Pass | 전체 패키지 빌드 종료 코드 `0` |
| D2-UI-RUN-003 | `pnpm test:e2e` | Pass | Playwright `5 passed (14.0s)` |
| D2-UI-RUN-004 | 모바일 직접 판정 `390x844` | Partial Pass | 랜딩/모드/랭킹/시트 Pass, 결과 CTA Fail |
| D2-UI-RUN-005 | 모바일 직접 판정 `360x800` | Fail | 랜딩 3개 CTA 동시 first-fold와 결과 CTA 구조 Fail |

## 5. 요청 항목별 결과
### 5.1 랜딩
- 결과: `Partial Pass`
- 확인 결과:
  - `390x844`
    - `start-button`, `mode-button`, `ranking-button` first-fold 내 노출: Pass
    - `start-button` Primary CTA: Pass
    - 보상 스탬프 3개, 마스코트 오브젝트, `daily-preview-card`, `personal-best-card` 노출: Pass
    - 임시 영문 문구/개발용 상태 문구: 미검출
  - `360x800`
    - `start-button`, `mode-button` first-fold: Pass
    - `ranking-button` first-fold: Fail
    - 보상 스탬프 3개, 마스코트 오브젝트, `daily-preview-card`, `personal-best-card` 노출: Pass
    - 임시 영문 문구/개발용 상태 문구: 미검출
- 판정:
  - 랜딩 Hero 캐주얼 강화 의도는 반영됐지만, `360x800`에서 CTA 3개 동시 first-fold 기준은 미충족이다.

### 5.2 모드 선택
- 결과: `Pass`
- 확인 결과:
  - `mode-button`으로 진입 가능
  - `mode-card-MARATHON`, `mode-card-SPRINT`, `mode-card-DAILY_CHALLENGE` 모두 노출
  - 포스터형 비주얼 `3개`, reward badge `3개`, 감정 카피 `3개` 확인
  - `mode-spotlight` 노출 확인
  - `mode-start-button-SPRINT`으로 선택 후 실제 `Sprint` 게임 진입 성공

### 5.3 랭킹
- 결과: `Pass`
- 확인 결과:
  - `ranking-button`으로 진입 가능
  - `ranking-screen`, `ranking-tab-MARATHON`, `ranking-segment-daily`, `ranking-row-current` 노출
  - 강조 행 `.ranking-row.highlighted` 1건 확인

### 5.4 결과 화면
- 결과: `Fail`
- 확인 결과:
  - `retry-button` Primary CTA: Pass
  - `390x844`
    - `retry-button` first-fold: Pass
    - `result-ranking-button`, `share-button`, `save-record-button` first-fold: Fail
    - Hero 주요 지표 first-fold: Pass
    - CTA grid 측정값 `338px`: 2x2가 아닌 1열
  - `360x800`
    - CTA 4개 모두 first-fold: Fail
    - `retry-button`조차 first-fold 미충족
    - Hero 주요 지표 first-fold: Pass
    - CTA grid 측정값 `308px`: 2x2가 아닌 1열
- 판정:
  - `retry-button` 우세는 충족했지만, 모바일 세로 CTA 4개 first-fold와 2x2 구조는 미충족이다.

### 5.5 바텀시트
- 결과: `Partial Pass`
- 확인 결과:
  - 닉네임 등록
    - `sheet-nickname`, `nickname-input`, `nickname-save-button` 노출: Pass
    - 닫기 후 기존 흐름 유지: Pass
  - Daily 상세
    - `sheet-daily` 노출: Pass
    - `daily-sheet-start-button` first-fold: Pass
    - 닫기 후 기존 흐름 유지: Pass
  - 설정
    - `sheet-settings` 노출: Pass
    - `sheet-contrast-toggle` 노출: Pass
    - `sheet-ghost-toggle` 미제공: Fail
    - `sheet-guide-toggle` 미제공: Fail
- 판정:
  - 바텀시트 구조 자체는 반영됐지만 설정 필수 옵션 구성이 부족하다.

## 6. 테스트케이스 매핑
| 테스트케이스 | 결과 | 비고 |
|---|---|---|
| `TC-001` | Pass | `390x844` 기준 충족 |
| `TC-001A` | Fail | `360x800`에서 CTA 3개 동시 first-fold 미충족 |
| `TC-015A` | Pass | Daily 시트 정상 |
| `TC-016` | Fail | CTA 4개 first-fold 미충족 |
| `TC-016A` | Fail | CTA 2x2 구조 미충족 |
| `TC-018A` | Pass | 닉네임 시트 정상 |
| `TC-020A` | Pass | 랭킹 기본 구조 정상 |
| `TC-026B` | Fail | 설정 시트 필수 옵션 부족 |

## 7. 종합 판정
- 개발자 요청 문서의 공식 명령 기준은 모두 `Pass`다.
- 그러나 화면 구조까지 포함한 제3자 시험 종합 판정은 `Partial Pass`다.
- 열려 있는 주요 항목:
  - `TC-001A`
  - `TC-016`
  - `TC-016A`
  - `TC-026B`

## 8. 개발자 전달 요약
- `FE-012` 랜딩 Hero 강화와 `FE-013` 모드 선택 포스터형 재디자인은 전반적으로 반영됐다.
- 모드 선택과 랭킹, 닉네임/Daily 바텀시트는 Pass다.
- 다만 `360x800` 랜딩 CTA 3개 first-fold, 결과 CTA 4개 first-fold, 결과 2x2 CTA 구조, 설정 시트 필수 옵션은 아직 미충족이다.
- 따라서 `FE-008 ~ FE-013` 범위의 최종 QA 판정은 `Partial Pass`다.

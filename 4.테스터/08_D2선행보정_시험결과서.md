# D2 선행보정 시험결과서

## 1. 문서 목적
- 본 문서는 `3.개발자/20_테스터전달_D2선행보정_시험요청.md` 기준으로 수행한 `GE-003`, `FE-004`, `FE-005`, `FE-006` 제3자 시험 결과를 기록한다.
- 본 시험은 D2 본 기능 `TC-009 ~ TC-011` 착수 전 선행 보정 범위를 검증하는 목적이다.

## 2. 실행 개요
- 실행 일시: 2026-03-12 00:41 KST ~ 2026-03-12 01:24 KST
- 실행 기준 문서:
  - `3.개발자/20_테스터전달_D2선행보정_시험요청.md`
  - `4.테스터/01_MVP_통합_테스트계획서.md`
  - `4.테스터/02_상세_테스트케이스.md`
- 실행 환경:
  - 워크스페이스 루트 명령 실행
  - 공개 웹 `http://127.0.0.1:3110`
  - 모바일 뷰포트 `360x800`
  - Playwright headless 브라우저 자동 조작

## 3. 수행 결과 요약
| 구분 | 결과 |
|---|---|
| `pnpm test` | Pass |
| `pnpm build` | Pass |
| `pnpm test:e2e` | Fail |
| 엔진 보정 회귀 확인 | Pass |
| 분석 이벤트 확인 | Pass |
| 모바일 세로 `360x800` 확인 | Pass |
| 접근성 옵션 확인 | Pass |
| 종합 판정 | `Partial Pass` |

## 4. 수행 테스트 상세
| 실행 ID | 대상 | 결과 | 비고 |
|---|---|---|---|
| D2-PRE-RUN-001 | `pnpm test` | Pass | `@tetris/game-engine` 16 tests passed |
| D2-PRE-RUN-002 | `pnpm build` | Pass | 루트 빌드 종료 코드 `0` |
| D2-PRE-RUN-003 | `pnpm test:e2e` | Fail | `config.webServer` 단계에서 `/admin` 페이지 수집 실패 |
| D2-PRE-RUN-004 | `GE-003` 엔진 회귀 확인 | Pass | combo/b2b/perfect clear/hold/ghost 관련 엔진 테스트 통과 |
| D2-PRE-RUN-005 | `FE-004` 분석 이벤트 확인 | Pass | `window.__tetrisAnalyticsEvents` 순서 확인 |
| D2-PRE-RUN-006 | `FE-005` 모바일 세로 `360x800` 확인 | Pass | 랜딩/플레이/결과 주요 UI 가시성 및 비겹침 확인 |
| D2-PRE-RUN-007 | `FE-006` 접근성 옵션 확인 | Pass | 고대비 토글, `aria-label`, `aria-live`, 가이드 노출 확인 |

## 5. 항목별 결과
### 5.1 `GE-003` 엔진 규칙 회귀 확인
- 결과: Pass
- 수행 근거:
  - `pnpm test` 통과
  - `@tetris/game-engine` 16개 테스트 전부 성공
  - `session.test.ts` 기준 아래 항목 통과
    - Hold swap 및 잠금 전 중복 Hold 차단
    - Hold 재호출 시 회전 상태 `0` 복귀
    - Ghost cell 렌더링
    - Perfect Clear 점수 반영
    - 연속 Quad에서 Combo 및 Back-to-Back 보너스 반영
    - Sprint 중력 레벨 고정
- 해석:
  - 이번 제3자 시험에서는 GE-003 보정 범위의 자동 회귀가 정상 동작함을 확인했다.

### 5.2 `FE-004` 분석 이벤트 확인
- 결과: Pass
- 수행 방식:
  - 랜딩 진입
  - `바로 시작`
  - 게임 시작
  - 세션 종료
  - `다시 시작`
  - 브라우저 콘솔에서 `window.__tetrisAnalyticsEvents` 확인
- 확인 결과:
  - 시작 직후 이벤트:
    - `landing_view`
    - `quick_start_click`
    - `game_start`
  - 재시작까지 포함한 최종 이벤트:
    - `landing_view`
    - `quick_start_click`
    - `game_start`
    - `game_finish`
    - `retry_click`
    - `game_start`
- 판정:
  - 요청 문서 기준 5개 이벤트가 정확한 이름으로 누적되므로 Pass

### 5.3 `FE-005` 모바일 세로 `360x800` 확인
- 결과: Pass
- 확인 항목:
  - 랜딩: `start-button`, `mode-button`, `contrast-toggle`, `analytics-last-event`
  - 플레이: `game-canvas`, `score-value`, `lines-value`, `level-value`, `hold-slot`, `next-queue`, `touch-controls`, `combo-status`, `b2b-status`, `touch-help`
  - 결과: `retry-button`, `share-button`, `save-record-button`, `result-ranking-button`, `rank-status`
- 확인 결과:
  - 위 항목은 모두 `360x800`에서 가시 상태였다.
  - 랜딩 버튼 간 겹침 없음
  - 플레이 `score-value`/`hold-slot`/`next-queue`/`touch-controls` 간 겹침 없음
  - 결과 CTA 간 겹침 없음
  - 랜딩 주요 CTA는 뷰포트 내에 모두 들어왔다.
- 판정:
  - 요청 문서 기준 모바일 세로 `360x800` 레이아웃은 Pass

### 5.4 `FE-006` 접근성 옵션 확인
- 결과: Pass
- 확인 항목:
  - 랜딩 `contrast-toggle`
  - 플레이 `playing-contrast-toggle`
  - `accessibility-guide`
  - `touch-controls`의 `aria-label`
  - 터치 버튼의 개별 `aria-label`
  - `rank-status`의 `aria-live`
- 확인 결과:
  - 랜딩 고대비 토글:
    - `data-contrast-mode` `default -> high`
    - `aria-pressed` `false -> true`
  - 플레이 고대비 토글:
    - `data-contrast-mode` `high -> default`
    - `aria-pressed` `true -> false`
  - `touch-controls` `aria-label=\"touch controls\"` 확인
  - 터치 버튼 6종 모두 스크린리더용 이름 존재
    - `왼쪽 조작`
    - `오른쪽 조작`
    - `회전 조작`
    - `홀드 조작`
    - `소프트 드롭 조작`
    - `하드 드롭 조작`
  - `accessibility-guide` 화면 노출 확인
  - `rank-status` `aria-live=\"polite\"` 확인
- 판정:
  - 요청 문서 기준 접근성 옵션은 Pass

## 6. 실패 항목
### 6.1 `pnpm test:e2e`
- 결과: Fail
- 오류 요약:
  - Playwright `config.webServer` 기동 단계에서 실패
  - 핵심 로그:
    - `PageNotFoundError: Cannot find module for page: /admin`
    - `Failed to collect page data for /admin`
    - `Process from config.webServer was not able to start. Exit code: 1`
- 영향:
  - 공식 E2E 게이트는 아직 통과로 볼 수 없다.
  - 수동/직접 브라우저 검증은 성공했지만, 릴리스 게이트 자동화는 닫히지 않았다.
- 조치 필요:
  - `playwright.config.ts`의 `webServer` 기동 경로와 `apps/web`의 `/admin` 페이지 수집 조건을 재확인해야 한다.

## 7. 종합 판정
| 항목 | 판정 |
|---|---|
| 자동화 명령 `pnpm test` | Pass |
| 자동화 명령 `pnpm build` | Pass |
| 자동화 명령 `pnpm test:e2e` | Fail |
| `GE-003` | Pass |
| `FE-004` | Pass |
| `FE-005` | Pass |
| `FE-006` | Pass |
| 종합 판정 | `Partial Pass` |

## 8. 개발자 전달 요약
- 선행 보정 기능 자체는 제3자 관점에서 대체로 정상이다.
- 특히 이벤트 5종, `360x800` 모바일 세로, 고대비/접근성 속성은 이번 시험에서 Pass다.
- 다만 개발자 요청서의 Pass 기준에는 `pnpm test:e2e` 종료 코드 `0`이 포함되어 있고, 현재 이 항목이 `/admin` 페이지 수집 실패로 깨진다.
- 따라서 이번 시험 결과는 `Partial Pass`로 전달하는 것이 맞다.
